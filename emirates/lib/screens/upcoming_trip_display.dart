import 'package:flutter/material.dart';

class UpcomingTripDisplay extends StatelessWidget {
  const UpcomingTripDisplay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}